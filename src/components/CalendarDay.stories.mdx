import { Meta, Story } from '@storybook/addon-docs'
import Component from '~/components/CalendarDay.vue'

<Meta
  title='Components/Calendar/Day'
  component={Component}
  argTypes={{
    hasEvent: { control: 'boolean' },
    offset: { control: 'number' },
  }}
/>

# Calendar Day

This component renders a calendar day tile.

## Usage

```html
<calendar-day :has-event="true" />
```

export const Template = (args) => ({
  components: { Component },
  setup() {
    return { args }
  },
  template: `<Component v-bind="args">Text</Component>`,
})

## Variants

### Default

<Story name='Default'>{Template.bind({})}</Story>

### Offset

<Story
  name='Offset'
  args={{
    offset: 1,
  }}
>
  {Template.bind({})}
</Story>

### Has Event

<Story
  name='Has Event'
  args={{
    hasEvent: true,
  }}
>
  {Template.bind({})}
</Story>

### Has Event w/ Offset

<Story
  name='Has Event w/ Offset'
  args={{
    hasEvent: true,
    offset: -1,
  }}
>
  {Template.bind({})}
</Story>
