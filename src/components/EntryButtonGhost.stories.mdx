import { Meta, Story } from '@storybook/addon-docs'
import Component from '~/components/EntryButtonGhost.vue'

<Meta
  title='Components/Entry/Ghost Button'
  component={Component}
  argTypes={{
    type: {
      options: ['default', 'danger'],
      control: { type: 'select' },
    },
    disabled: { control: 'boolean' },
  }}
/>

# Button

This component renders a generic ghost button.

## Usage

```html
<entry-button-ghost>Text</entry-button-ghost>
```

export const Template = (args) => ({
  components: { Component },
  setup() {
    return { args }
  },
  template: `<Component v-bind="args">Text</Component>`,
})

## Variants

### Default / Primary

<Story name='Default'>{Template.bind({})}</Story>

### Danger

<Story
  name='Danger'
  args={{
    type: 'danger',
  }}
>
  {Template.bind({})}
</Story>

### Disabled

<Story
  name='Disabled'
  args={{
    disabled: true,
  }}
>
  {Template.bind({})}
</Story>
