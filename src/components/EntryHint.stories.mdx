import { Meta, Story } from '@storybook/addon-docs'
import Component from '../components/EntryHint.vue'

<Meta
  title='Components/Entry/Hint'
  component={Component}
  argTypes={{
    hintText: { control: 'text' },
    hasSuccess: { control: 'boolean' },
    hasError: { control: 'boolean' },
    disabled: { control: 'boolean' },
  }}
/>

# Hint

This component renders hint text for inputs.

## Usage

```html
<entry-hint hint-text="Text" />
```

export const Template = (args) => ({
  components: { Component },
  setup() {
    return { args }
  },
  template: `<Component v-bind="args" />`,
})

## Variants

### Default

<Story
  name='Default'
  args={{
    hintText: 'Text',
  }}
>
  {Template.bind({})}
</Story>

### Success

<Story
  name='Success'
  args={{
    hintText: 'Text',
    hasSuccess: true,
  }}
>
  {Template.bind({})}
</Story>

### Error

<Story
  name='Error'
  args={{
    hintText: 'Error Text',
    hasError: true,
  }}
>
  {Template.bind({})}
</Story>

### Disabled

<Story
  name='Disabled'
  args={{
    hintText: 'Text',
    disabled: true,
  }}
>
  {Template.bind({})}
</Story>
