import { Meta, Story } from '@storybook/addon-docs'
import Component from '~/components/EntrySelect.vue'

<Meta
  title='Components/Entry/Select'
  component={Component}
  argTypes={{
    label: { control: 'text' },
    for: { control: 'text' },
    statusMap: { control: 'object' },
  }}
/>

# Select Entry

This component renders a select input with a label and programmable hint text.

## Usage

```html
<entry-select
  label="Options"
  for="email"
  placeholder="Select an option"
>
  <option value="option-1">Option 1</option>
  <option value="option-2">Option 2</option>
  <option value="option-3">Option 3</option>
</entry-select>
```

export const Template = (args) => ({
  components: { Component },
  setup() {
    return { args }
  },
  template: `<Component v-bind="args">
  <option value="option-1">Option 1</option>
  <option value="option-2">Option 2</option>
  <option value="option-3">Option 3</option>
</Component>`,
})

## Variants

### Default

<Story
  name='Default'
  args={{
    label: 'Options',
    for: 'storybook',
    placeholder: 'Select an option',
    statusMap: {
      default: [[() => true, 'This is a hint']],
    },
  }}
>
  {Template.bind({})}
</Story>

### Disabled

<Story
  name='Disabled'
  args={{
    label: 'Options',
    for: 'storybook',
    placeholder: 'Select an option',
    statusMap: {
      disabled: [[() => true, 'This is a disabled input']],
    },
  }}
>
  {Template.bind({})}
</Story>
