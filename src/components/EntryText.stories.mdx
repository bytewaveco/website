import { Meta, Story } from '@storybook/addon-docs'
import Component from '../components/EntryText.vue'

<Meta
  title='Components/Entry/Text'
  component={Component}
  argTypes={{
    hintText: { control: 'text' },
    hasSuccess: { control: 'boolean' },
    hasError: { control: 'boolean' },
    disabled: { control: 'boolean' },
  }}
/>

# Hint

This component renders a text input.

## Usage

```html
<entry-text
  label="Text"
  placeholder="Placeholder Text"
  :statusMap="{
    default: [() => [true, 'Hint Text']],
  }"
/>
```
