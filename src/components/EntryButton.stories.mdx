import { Meta, Story } from '@storybook/addon-docs'
import Component from '~/components/EntryButton.vue'

<Meta
  title='Components/Entry/Button'
  component={Component}
  argTypes={{
    type: {
      options: ['default', 'secondary', 'danger'],
      control: { type: 'select' },
    },
    disabled: { control: 'boolean' },
  }}
/>

# Button

This component renders a generic button.

## Usage

```html
<entry-button>Text</entry-button>
```

export const Template = (args) => ({
  components: { Component },
  setup() {
    return { args }
  },
  template: `<Component v-bind="args">Text</Component>`,
})

## Variants

### Default / Primary

<Story name='Default'>{Template.bind({})}</Story>

### Secondary

<Story
  name='Secondary'
  args={{
    type: 'secondary',
  }}
>
  {Template.bind({})}
</Story>

### Danger

<Story
  name='Danger'
  args={{
    type: 'danger',
  }}
>
  {Template.bind({})}
</Story>

### Disabled

<Story
  name='Disabled'
  args={{
    disabled: true,
  }}
>
  {Template.bind({})}
</Story>
