import { Meta, Story } from '@storybook/addon-docs'
import Component from '../components/EntryLabel.vue'

<Meta
  title='Components/Entry/Label'
  component={Component}
  argTypes={{
    label: { control: 'text' },
    labelFor: { control: 'text' },
    disabled: { control: 'boolean' },
  }}
/>

# Hint

This component renders label text for inputs.

## Usage

```html
<entry-label
  label-for="input"
  label="Text"
/>
```

export const Template = (args) => ({
  components: { Component },
  setup() {
    return { args }
  },
  template: `<Component v-bind="args" label="Text" />`,
})

## Variants

### Default

<Story name='Default'>{Template.bind({})}</Story>

### Disabled

<Story
  name='Disabled'
  args={{
    disabled: true,
  }}
>
  {Template.bind({})}
</Story>
