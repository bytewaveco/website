import { Meta, Story } from '@storybook/addon-docs'
import Component from '../components/EntryButtonIcon.vue'

<Meta
  title='Components/Entry/Icon Button'
  component={Component}
  argTypes={{
    icon: { control: 'text' },
    disabled: { control: 'boolean' },
  }}
/>

# Icon Button

This component renders a button containing an icon from [Phosphor Icons](https://icones.js.org/collection/ph).

## Usage

```html
<entry-button-icon icon="bell" />
```

export const Template = (args) => ({
  components: { Component },
  setup() {
    return { args }
  },
  template: `<Component v-bind="args" />`,
})

## Variants

### Default

<Story name='Default'>{Template.bind({})}</Story>

### Disabled

<Story
  name='Disabled'
  args={{
    disabled: true,
  }}
>
  {Template.bind({})}
</Story>
