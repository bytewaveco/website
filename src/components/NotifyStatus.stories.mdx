import { Meta, Story } from '@storybook/addon-docs'
import Component from '../components/NotifyStatus.vue'

<Meta
  title='Components/Notify/Status'
  component={Component}
  argTypes={{
    type: { control: 'select', options: ['live', 'new'] },
  }}
/>

# Notification Status

This component renders a notification status badge.

## Usage

```html
<notify-status type="live" />
```

export const Template = (args) => ({
  components: { Component },
  setup() {
    return { args }
  },
  template: `<Component v-bind="args" />`,
})

## Variants

### Default

<Story name='Default'>{Template.bind({})}</Story>

### New

<Story
  name='New'
  args={{
    type: 'new',
  }}
>
  {Template.bind({})}
</Story>
